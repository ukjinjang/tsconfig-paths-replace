import { beep } from '@beeper';
import { sum } from '@math/sum';
import { subtract } from '@math/subtract';
import { magic } from '@magic';

const a = 1, b = 2;

console.log(beep());
console.log(sum(a, b));
console.log(subtract(a, b));
console.log(magic(a, b));
